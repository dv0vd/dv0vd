cert=/app/letsencrypt/live/$BASE_URL/fullchain.pem
pkey=/app/letsencrypt/live/$BASE_URL/privkey.pem
listening-port=$COTURN_PORT
tls-listening-port=$COTURN_TLS_PORT
min-port=$COTURN_MIN_PORT
max-port=$COTURN_MAX_PORT
external-ip=$PUBLIC_IP
fingerprint
use-auth-secret
static-auth-secret=$COTURN_SECRET
realm=$COTURN_HOST
verbose
log-file=stdout
no-multicast-peers
no-cli